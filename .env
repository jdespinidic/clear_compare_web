# Base URL of the embedded RateMatch application form used by the /apply/* pages
# (personal, car, home). Override in Vercel project settings or .env.local to
# point the forms at a different endpoint without a code change.
NEXT_PUBLIC_RATEMATCH_FORM_URL=https://forms.ratematch.ai/
